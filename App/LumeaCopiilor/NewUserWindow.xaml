<Window x:Class="LumeaCopiilor.NewUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        mc:Ignorable="d"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LumeaCopiilor"
        Title="Adăugați utilizator"
        WindowState="Maximized"
        Background="{StaticResource LightColor}">

    <ScrollViewer VerticalScrollBarVisibility="Auto"
                  HorizontalScrollBarVisibility="Disabled">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Style="{StaticResource H1}"
                       Text="Adăugați"
                       HorizontalAlignment="Center"
                       Margin="0,80,0,48"/>

            <Grid Grid.Row="1"
                  Margin="80,0,80,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="32"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0">
                    <TextBlock Text="Introduceți numele de utilizator"
                               Style="{StaticResource P}"
                               Margin="0,0,0,8"/>
                    <TextBox x:Name="UsernameInput"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <TextBlock Text="Introduceți loginul"
                               Style="{StaticResource P}"
                               Margin="0,0,0,8"/>
                    <TextBox x:Name="LoginInput"/>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="2"
                Margin="80,24,80,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="32"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0"
                Grid.ColumnSpan="3"
                HorizontalAlignment="Center"
                Width="300">
                    <TextBlock Text="Introduceți parola"
                   Style="{StaticResource P}"
                   Margin="0,0,0,8"/>
                    <TextBox x:Name="PasswordInput"/>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="3"
                  Margin="80,32,80,0">
                <Button Style="{StaticResource SecondaryButton}"
                        Content="Anulare"
                        HorizontalAlignment="Left" Click="Button_Click"/>
                <Button Style="{StaticResource PrimaryButton}"
                        Content="Adăugați"
                        HorizontalAlignment="Right"/>
            </Grid>

            <Image Grid.Row="4"
       Source="/Assets/bottom-img.png"
       Stretch="Uniform"
       VerticalAlignment="Bottom"
       HorizontalAlignment="Stretch"/>

        </Grid>
    </ScrollViewer>
</Window>